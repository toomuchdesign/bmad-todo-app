# Local development defaults
#
# Start Postgres:
#   docker compose up -d
#
# Database connection string used by the API workspace scripts.
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/bmad_todo

# Optional: use a separate DB for tests once tests start using the DB.
# DATABASE_URL_TEST=postgresql://postgres:postgres@127.0.0.1:5432/bmad_todo_test
